package header_const

// headers keys
const (
	WebHeaderKeyContentType = "Content-Type"
	WebHeaderKeyAuthorization = "Authorization"
)

// header values
const (
	WebHeaderValueContentTypeJson = "application/json"
)
